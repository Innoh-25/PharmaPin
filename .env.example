# Example environment variables for PharmaPin

# MongoDB connection string (required in production)
MONGODB_URI=mongodb+srv://InnohDev:S12IdPi91610673.@cluster0.y7t8sib.mongodb.net/PharmaPin?appName=Cluster0

# JWT secret for signing tokens
JWT_SECRET=S12IdPi91610673.

# Backend port (optional)
PORT=5000

# Node environment
NODE_ENV=production

# Frontend origin (used for CORS in production)
FRONTEND_ORIGIN=http://localhost:5173

# Vite env for frontend to call API in production (must include VITE_ prefix)
VITE_API_URL=https://api.example.com

